# 핵심 의존성
streamlit==1.28.2
nest-asyncio==1.5.8
requests==2.31.0
aiohttp==3.9.1
pandas==2.2.3  # Python 3.13 호환 버전
python-dotenv==1.0.1
openai>=1.0.0  # GPT-5 지원
numpy==1.26.4  # pandas와 호환

# 추가 보안 및 성능 최적화
asyncio  # 비동기 처리 (기본 포함)
typing  # 타입 힌트 (기본 포함)
